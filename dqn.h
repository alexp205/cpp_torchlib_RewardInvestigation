#pragma once

#ifndef NETS_H_
#define NETS_H_

#include <torch/torch.h>
#include <vector>

using namespace torch;

typedef vector<double> vd;

/*
class ValueNet
{
    int a;

    public:
    ValueNet(int);
    //ValueNet(const ValueNet&);
    //ValueNet& operator=(const ValueNet&);
    //~ValueNet();
    int forward(int);
    void save(int);
    int load(int);
    void print();
}
*/

#endif
